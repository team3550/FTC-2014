//code to display things on the NXT display

/*  TODO: fix array input
void displayMenuItems(string items[]) {
  //displays a list of items, defined by the input list
}
*/

int displaySelectMenuItems(string items[]) {
  displayMenuItems(string items[]);
  //let the user select a specific item
  return itemNumber;
}
